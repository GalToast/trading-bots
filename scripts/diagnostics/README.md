# Diagnostics

Manual health checks, state probes, watchdog checks, and debug readers live here.

These scripts are operational support surfaces. The canonical runtime remains at the repo root: `mt5_bot.py`, `mt5_bot_v10.py`, `monitor.py`, `exit_manager.py`, `brain.py`, and the config files.
